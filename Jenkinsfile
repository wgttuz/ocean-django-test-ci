pipeline {
    agent any
    
    stages {
        stage('Print user info') {
            steps {
                sh 'echo "Current user: $USER"'
            }
        }
        
        // Добавьте другие этапы вашей сборки здесь
    }
}
